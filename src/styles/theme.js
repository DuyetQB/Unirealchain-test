const colors = {
  blue: '#557ef8',
 
  darkGrey: '#333333',
  gray0: '#777777',

  white: '#fff',
  black: '#080808',
  border: '#E0E0E0',
  
};

const fontSize = {
  xs: '10px',
  sm: '12px',
  mdsm:'13px',
  base: '14px',
  md: '16px',
  lg: '18px',
  xl: '20px',
  exl: '22px',
  xxl: '24px',
  xxxl: '40px',
};

const fontWeight = {
  regular: 400,
  medium: 500,
  bold: 600,
  extraBold: 700,
};

const zIndex = {
  base: 1,
  dropdown: 10,
  backdrop: 50,
  modal: 100,
};

const boxShadow = {
  box: '0px 5px 14px 11px rgba(0, 0, 0, 0.1)',
  boxWrapper: '0px 4px 16px rgba(0, 0, 0, 0.06);',
  grayBox: '0px 5px 14px 11px rgba(0, 0, 0, 0.1)',
};

const borderRadius = {
  base: '4px',
  md: '8px',
  xl:'40px'
};

export default {
  colors,
  fontSize,
  fontWeight,
  zIndex,
  boxShadow,
  borderRadius,
};
